# OMC / tooling state
.omc
.claude

# impeccable (design-tooling) local artifacts — keep OUT of the public repo:
# PRODUCT.md documents the deception-study design; .impeccable/ holds local
# critique snapshots. Neither is needed for the app/Pages build.
.impeccable
PRODUCT.md

# Dependencies
node_modules

# Build outputs
dist
release
out

# Electron build artifacts
*.dmg
*.exe
*.deb
*.AppImage
*.snap
*.pkg
*.blockmap

# Vite cache
.vite
.cache

# Editor / OS
.DS_Store
Thumbs.db
*.log
.idea
.vscode/*
!.vscode/extensions.json

# Env
.env
.env.local
.env.*.local

# Course reference material (large PDFs, not app code)
lecture_note
